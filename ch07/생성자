생성자라는게 있는데 클래스를 선언할 떄 명시적으로 생성자를 구현하지 않아도 컴파일러가 생성자를 만들어 준다.
그런데 왜 구현해야 하느냐 하면은 객체를 다루다 보면 객체를 생성하는 시점에 객체의 상태를 즉 객체의 필드를 원하는 값으로 초기화
하고 싶을 때가 있다. 매개변수를 입력받아 원하는 값으로 필드를 초기화할 수 있게 하는 것이 생성자이다. 
예를 들어 게임에서 닉네임은 다 다르지만 모두가 1레벨에서 시작하듯이 말이다.

매개변수가 없는 버전의 생성자는 컴파일러가 자동으로 생성해준 생성자를 호출할 때처럼 쓰고 
매개변수가 있는 버전은 생성자의 괄호 안에 필요한 인수를 입력하면 된다
아래는 그 예제 

~~~
using System;

class Cat
{
  public string Name;
  public string Color;
  public Cat() //생성자 클래스 이름과 똑같이 써야한다. 반환 형식은 없다.
  {
    Name = "";
    Color = "";
  }
  public Cat(string _Name, string _Color) //객체를 생성할 때 이름과 색을 입력받아 초기화한다.
  {
    Name = _Name;
    Color = _Color;
  }
  
}

class Program
{
  static void Main(string[] args)
  {
    Cat cat1 = new Cat(); //기본 생성자 호출
    cat1.Name = "나비";
    cat1.Color = "검정색";
    Console.WriteLine("고양이 이름: " + cat1.Name);
    Console.WriteLine("고양이 색깔: " + cat1.Color);

    Cat cat2 = new Cat("모카", "갈색"); //매개변수가 있는 생성자 호출
    Console.WriteLine("고양이 이름: " + cat2.Name);
    Console.WriteLine("고양이 색깔: " + cat2.Color);
  }
}
~~~
